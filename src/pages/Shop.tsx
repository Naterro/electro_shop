import React from 'react';
import DeviceList from "../components/DeviceList";

const Shop = () => {
    return (
        <div>
            <DeviceList children={null}/>
        </div>
    );
};

export default Shop;